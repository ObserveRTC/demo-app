import React, { useRef, useEffect } from 'react';
import LocalClientMediaBox from './LocalClientMediaBox';

const LocalClient: React.FC = () => {
  useEffect(() => {

    return () => {
    };
  }, []);

  return (
    <div>
		  <LocalClientMediaBox />
    </div>
  );
};

export default LocalClient;
